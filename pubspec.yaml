name: barcode_store
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.10.1 <4.0.0' # SDK aralığını belirtmek daha güvenlidir

dependencies:
  flutter:
    sdk: flutter

  # Veritabanı ve durum yönetimi paketleri
  sqflite: ^2.3.0
  path: ^1.9.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # Projenin temiz kalması için güncel tutulmalı

flutter:
  uses-material-design: true
  # Eğer resim veya asset kullanacaksan buradaki girintiye dikkat etmelisin:
  # assets:
  #   - assets/images/